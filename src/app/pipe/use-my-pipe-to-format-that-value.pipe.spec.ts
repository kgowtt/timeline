import { UseMyPipeToFormatThatValuePipe } from './use-my-pipe-to-format-that-value.pipe';

describe('UseMyPipeToFormatThatValuePipe', () => {
  it('create an instance', () => {
    const pipe = new UseMyPipeToFormatThatValuePipe();
    expect(pipe).toBeTruthy();
  });
});
